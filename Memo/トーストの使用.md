# Toastの使い方
```bash
import { useToast } from '@/components/common/ToastContext.jsx'

  const {
    showSuccessToast,
    showErrorToast,
    showWarningToast,
    showInfoToast,
  } = useToast()

  showSuccessToast('保存しました')
  showErrorToast('保存に失敗しました')
```

# 必要な準備
👉 これを忘れると useToast は必ずエラーになります。
```App.jsx
import { ToastProvider } from '@/components/common/ToastContext.jsx'

function App() {
  return (
    <ToastProvider>
      {/* 既存のアプリ */}
    </ToastProvider>
  )
}
export default App
```