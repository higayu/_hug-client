# 原因として確認が必要な内容

## 1) キーボード入力イベントが止められていないか確認する

textareaにフォーカスでき、範囲選択や貼り付けが可能なのに手入力だけできない場合、`keydown`、`keypress`、`beforeinput` などのイベントがどこかで止められている可能性があります。

特に `preventDefault()` が実行されていると、通常の文字入力だけが反映されないことがあります。

---bash
確認するキーワード:
preventDefault
keydown
keypress
keyup
beforeinput
input
addEventListener
---endbash

## 2) フロント側のイベント処理を確認する

HTML側、JavaScript側、またはフレームワーク側で、document全体や親要素に対してキーボードイベントを監視していないか確認します。

例えば、以下のような処理があると、textareaへの入力が妨げられることがあります。

---bash
document.addEventListener('keydown', (e) => {
  e.preventDefault();
});
---endbash

textareaだけでなく、`window`、`document`、親コンポーネント、モーダル、ショートカット処理なども確認対象です。

## 3) Electron側の入力制御を確認する

Electronでは、メインプロセス側でキーボード入力を制御している場合があります。

特に `before-input-event` で `preventDefault()` を呼んでいると、フロント側のtextareaに文字が入力されなくなることがあります。

---bash
確認するキーワード:
webContents.on('before-input-event')
event.preventDefault()
globalShortcut.register
Menu.setApplicationMenu
accelerator
---endbash

## 4) グローバルショートカットが入力を奪っていないか確認する

`globalShortcut.register()` を使っている場合、特定のキー入力がElectronアプリ全体で処理され、textareaに届かないことがあります。

特に、単独キーや広範囲のショートカットを登録している場合は注意が必要です。

---bash
確認する例:
globalShortcut.register('A', ...)
globalShortcut.register('CommandOrControl+A', ...)
globalShortcut.register('Space', ...)
globalShortcut.register('Enter', ...)
---endbash

## 5) textareaがreadonlyまたはdisabledになっていないか確認する

`readonly` や `disabled` が設定されていると、入力できません。

ただし、貼り付けや選択の挙動によっては、見た目だけでは判断しにくい場合があります。

---bash
確認する属性:
readonly
disabled
aria-disabled
contenteditable
---endbash

DevToolsで対象のtextareaを選択し、属性が付いていないか確認します。

## 6) JavaScriptで入力値が即座に戻されていないか確認する

実際には入力イベントが発生していても、JavaScriptによって値が空文字や固定値に戻されている場合があります。

この場合、ユーザーから見ると「入力できない」ように見えます。

---bash
確認するキーワード:
textarea.value =
setState
useState
v-model
:value
value=
onInput
onChange
---endbash

React、Vue、Svelteなどを使っている場合は、状態管理側の値が正しく更新されているかも確認します。

## 7) CSSで入力文字が見えなくなっていないか確認する

文字自体は入力されているが、CSSの影響で見えていない可能性があります。

例えば、文字色が透明、背景色と文字色が同じ、フォントサイズが極端に小さいなどです。

---bash
確認するCSS:
color
background-color
opacity
font-size
line-height
caret-color
visibility
display
---endbash

特に以下のような指定がないか確認します。

---bash
textarea {
  color: transparent;
  caret-color: transparent;
}
---endbash

## 8) フォーカスが別要素に移動していないか確認する

textareaをクリックしているつもりでも、別の透明な要素やオーバーレイが前面にあり、入力先が想定と違っている場合があります。

DevToolsのConsoleで、現在フォーカスされている要素を確認します。

---bash
document.activeElement
---endbash

textareaが返ってこない場合、フォーカス制御や画面上の重なりを確認します。

## 9) オーバーレイやドラッグ領域の指定を確認する

Electronでは、独自タイトルバーやドラッグ可能領域を作るために `-webkit-app-region: drag;` を使うことがあります。

この指定がtextareaやその親要素にかかっていると、入力操作に影響する場合があります。

---bash
確認するCSS:
-webkit-app-region: drag;
-webkit-app-region: no-drag;
pointer-events
z-index
position
---endbash

入力欄には明示的に `no-drag` を指定します。

---bash
textarea,
input,
button {
  -webkit-app-region: no-drag;
}
---endbash

## 10) IMEや日本語入力だけの問題か確認する

日本語入力だけできないのか、英数字も入力できないのかを切り分けます。

英数字も入力できない場合は、IMEよりもイベント制御やElectron側の入力処理が原因である可能性が高いです。

---bash
確認する内容:
英数字は入力できるか
日本語だけ入力できないか
別のIMEでも同じか
OSを変えても同じか
Electronのバージョンで差があるか
---endbash

## 11) DevToolsでイベントの流れを確認する

DevToolsのConsoleで、入力時にどのイベントが発生しているか確認します。

---bash
document.addEventListener('keydown', e => {
  console.log('keydown', e.key, e.defaultPrevented);
}, true);

document.addEventListener('beforeinput', e => {
  console.log('beforeinput', e.data, e.defaultPrevented);
}, true);

document.addEventListener('input', e => {
  console.log('input', e.target.value);
}, true);
---endbash

`keydown` は出るが `beforeinput` や `input` が出ない場合、入力が途中で止められている可能性があります。

## 12) 最初に疑うべき原因

今回のように、範囲選択と貼り付けは可能だが手入力だけできない場合、最も疑うべき原因は次のどちらかです。

---bash
1. フロント側のkeydownやbeforeinputでpreventDefault()している
2. Electron側のwebContents.on('before-input-event')でpreventDefault()している
---endbash

まずはこの2点を優先して確認します。