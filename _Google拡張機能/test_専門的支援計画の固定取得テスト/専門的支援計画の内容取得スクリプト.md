１．https://www.hug-ayumu.link/hug/wm/addition_plan_situation.php?mode=list&c_id={SELECT_CHILLED}　へリクエストしてhtml取得

（92）の固定値でテストする


取得したhtmlに対して以下と同じ取得結果が欲しい
    try {
      const table = document.querySelector("div.individualSituation table.table");
      if (!table) {
        console.warn("テーブル未存在");
        return;
      }

      // 「公開」を含む <i> を探す
      const openLabel = [...table.querySelectorAll("i")]
        .find(el => el.textContent.trim() === "公開");

      if (!openLabel) {
        console.warn("公開ラベル未存在");
        return;
      }

      // 親の <a> を取得
      const link = openLabel.closest("a");
      if (!link) {
        console.warn("公開リンク未存在");
        return;
      }

      console.log("公開ページへ遷移", link.href);

      // 🔽ここがゴールidを取得（681） 
      link;

    } catch (e) {
      console.error("error:", e);
    }

-------------------
<a href="./addition_plan.php?mode=detail&amp;id=681" class="tdn dib por">
<span class="label create m5">作成日：2025年08月30日<i>公開</i></span>
</a>

２．URLの以下を作成し、htmlをリクエスト
https://www.hug-ayumu.link/hug/wm/addition_plan.php?mode=detail&id={SELECT_ID}　へリクエストしてhtml取得

リクエスト結果から id="carebreak" を抽出

コンソールログに出力